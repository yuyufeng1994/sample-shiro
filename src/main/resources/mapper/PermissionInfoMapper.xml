<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.yuyufeng.learn.shiro.dao.PermissionInfoMapper">

    <resultMap id="BaseResultMap" type="top.yuyufeng.learn.shiro.orm.po.PermissionInfo">
        <result property="permissionId" column="permission_id" javaType="java.lang.Long" jdbcType="BIGINT"/>
        <result property="permissionName" column="permission_name" javaType="java.lang.String" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
    permission_id, permission_name
  </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from permission_info
        where permission_id = #{permissionId,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from permission_info
    where permission_id = #{permissionId,jdbcType=BIGINT}
  </delete>

    <insert id="insert" parameterType="top.yuyufeng.learn.shiro.orm.po.PermissionInfo">
    insert into permission_info (permission_id, permission_name)
    values (#{permissionId,jdbcType=BIGINT}, #{permissionName,jdbcType=VARCHAR})
  </insert>
    <insert id="insertSelective" parameterType="top.yuyufeng.learn.shiro.orm.po.PermissionInfo">
        insert into permission_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="permissionId != null">
                permission_id,
            </if>
            <if test="permissionName != null">
                permission_name,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="permissionId != null">
                #{permissionId,jdbcType=BIGINT},
            </if>
            <if test="permissionName != null">
                #{permissionName,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="top.yuyufeng.learn.shiro.orm.po.PermissionInfo">
        update permission_info
        <set>
            <if test="permissionName != null">
                permission_name = #{permissionName,jdbcType=VARCHAR},
            </if>
        </set>
        where permission_id = #{permissionId,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="top.yuyufeng.learn.shiro.orm.po.PermissionInfo">
    update permission_info
    set permission_name = #{permissionName,jdbcType=VARCHAR}
    where permission_id = #{permissionId,jdbcType=BIGINT}
  </update>

</mapper>